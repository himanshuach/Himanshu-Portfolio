/**
 * LEGACY DATA — archived during Figma redesign (August 2026)
 * Active data lives in `data/portfolio.ts`
 *
 * The exports below are preserved for reference only and are not imported
 * by the new application.
 */

/*
import { links } from "@/config";

export const navItems = [
  { name: "About", link: "#about" },
  { name: "Projects", link: "#projects" },
  { name: "Experience", link: "#experience" },
  { name: "Contact", link: "#contact" },
] as const;

export const gridItems = [ ... ] as const;
export const projects = [ ... ] as const;
export const workExperience = [ ... ] as const;
export const socialLinks = [ ... ] as const;
export const approach = [ ... ] as const;
export const recentProjects = [ ... ] as const;
export const skills = [ ... ] as const;
export const certifications = [ ... ] as const;
*/

export {};
